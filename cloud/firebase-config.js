// Firebase config — replace with values from YOUR Firebase project.
//
// How to get these:
//   1. Open https://console.firebase.google.com
//   2. Create a new project (or pick an existing one)
//   3. In the project, click the Web icon (</>) to add a web app
//   4. Firebase shows you a "firebaseConfig" object — paste those values below
//
// These values are PUBLIC by design. Security is enforced by Firestore rules,
// not by hiding the config. It's safe to commit this file to GitHub.

export const firebaseConfig = {
  apiKey: "REPLACE_ME",
  authDomain: "REPLACE_ME.firebaseapp.com",
  projectId: "REPLACE_ME",
  storageBucket: "REPLACE_ME.appspot.com",
  messagingSenderId: "REPLACE_ME",
  appId: "REPLACE_ME"
};
